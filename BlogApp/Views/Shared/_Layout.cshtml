@using Microsoft.AspNetCore.Identity;
@using BlogApp.Data.Models
@inject SignInManager<ApplicationUser> SignInManager

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - ВашБлог</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/BlogApp.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/auth.css" />
    <link rel="stylesheet" href="~/css/mainpage.css" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <div class="container-fluid d-flex justify-content-between align-items-center">

                <!-- Логотип -->
                <span class="navbar-brand"
                      onclick="location.href='@Url.Action("Index", "Home")'"
                      style="cursor: pointer; font-size: 1.8rem; font-weight: 700;">
                    ВашБлог
                </span>

                @if (SignInManager.IsSignedIn(User))
                {
                    <div class="d-flex align-items-center w-100 justify-content-between">

                        <!-- Меню -->
                        <div class="d-flex align-items-center gap-4 flex-grow-1 ps-3">
                            <span class="menu-span" onclick="location.href='@Url.Action("Index", "Comment")'">Комментарии</span>
                            <span class="menu-span" onclick="location.href='@Url.Action("Index", "Tag")'">Теги</span>
                            @if (User.IsInRole("Admin"))
                            {
                                <span class="menu-span" onclick="location.href='@Url.Action("Index", "User")'">Пользователи</span>
                                <span class="menu-span" onclick="location.href='@Url.Action("Index", "Role")'">Роли</span>
                            }
                            <span class="menu-span" onclick="location.href='@Url.Action("Index", "Post")'">Статьи</span>
                        </div>

                        <!-- Профиль + выход -->
                        <div class="d-flex align-items-center gap-3">
                            <span class="menu-span" onclick="location.href='@Url.Action("Profile", "Auth")'">Мой профиль</span>
                            <form asp-controller="Auth" asp-action="Logout" method="post" class="m-0">
                                <button type="submit" class="logoutButton">Выход</button>
                            </form>
                        </div>
                    </div>
                }
            </div>
        </nav>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2025 - BlogApp - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
        </div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
</body>
</html>
